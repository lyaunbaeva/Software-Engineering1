# 📡 API Документация: Калькулятор

## Описание

REST API для выполнения математических операций. API предоставляет интерфейс для внешних приложений, позволяющий выполнять вычисления через HTTP запросы.

## Авторизация

Все запросы (кроме `/api/health`) требуют API-ключа в заголовке запроса:

```
X-API-Key: secret_key_12345
```

**По умолчанию API-ключ:** `secret_key_12345`

Для изменения API-ключа установите переменную окружения:
```bash
export CALCULATOR_API_KEY=your_secret_key
```

## Эндпоинты

### 1. Проверка работоспособности

**GET** `/api/health`

Проверяет, что API работает. Не требует авторизации.

**Пример запроса:**
```bash
curl http://localhost:5000/api/health
```

**Пример ответа:**
```json
{
  "status": "ok",
  "message": "API калькулятора работает",
  "version": "1.0"
}
```

---

### 2. Получение списка операций

**GET** `/api/operations`

Возвращает список всех доступных математических операций.

**Заголовки:**
```
X-API-Key: secret_key_12345
```

**Пример запроса:**
```bash
curl -H "X-API-Key: secret_key_12345" \
     http://localhost:5000/api/operations
```

**Пример ответа:**
```json
{
  "operations": [
    {
      "name": "addition",
      "symbol": "+",
      "description": "Сложение двух чисел",
      "endpoint": "/api/calculate",
      "example": {
        "operation": "add",
        "a": 10,
        "b": 5
      }
    },
    {
      "name": "subtraction",
      "symbol": "-",
      "description": "Вычитание второго числа из первого",
      "endpoint": "/api/calculate",
      "example": {
        "operation": "subtract",
        "a": 10,
        "b": 5
      }
    },
    {
      "name": "multiplication",
      "symbol": "*",
      "description": "Умножение двух чисел",
      "endpoint": "/api/calculate",
      "example": {
        "operation": "multiply",
        "a": 10,
        "b": 5
      }
    },
    {
      "name": "division",
      "symbol": "/",
      "description": "Деление первого числа на второе",
      "endpoint": "/api/calculate",
      "example": {
        "operation": "divide",
        "a": 10,
        "b": 5
      }
    },
    {
      "name": "power",
      "symbol": "^",
      "description": "Возведение первого числа в степень второго",
      "endpoint": "/api/calculate",
      "example": {
        "operation": "power",
        "a": 2,
        "b": 3
      }
    }
  ],
  "total": 5
}
```

---

### 3. Выполнение вычисления

**POST** `/api/calculate`

Выполняет математическую операцию над двумя числами.

**Заголовки:**
```
Content-Type: application/json
X-API-Key: secret_key_12345
```

**Тело запроса:**
```json
{
  "operation": "add",
  "a": 10,
  "b": 5
}
```

**Доступные операции:**
- `add` - сложение
- `subtract` - вычитание
- `multiply` - умножение
- `divide` - деление
- `power` - возведение в степень

**Пример запроса (сложение):**
```bash
curl -X POST http://localhost:5000/api/calculate \
     -H "Content-Type: application/json" \
     -H "X-API-Key: secret_key_12345" \
     -d '{
       "operation": "add",
       "a": 10,
       "b": 5
     }'
```

**Пример ответа:**
```json
{
  "result": 15,
  "operation": "add",
  "a": 10,
  "b": 5,
  "expression": "10 + 5 = 15",
  "id": 1
}
```

**Пример запроса (деление):**
```bash
curl -X POST http://localhost:5000/api/calculate \
     -H "Content-Type: application/json" \
     -H "X-API-Key: secret_key_12345" \
     -d '{
       "operation": "divide",
       "a": 15,
       "b": 3
     }'
```

**Пример ответа:**
```json
{
  "result": 5,
  "operation": "divide",
  "a": 15,
  "b": 3,
  "expression": "15 / 3 = 5",
  "id": 2
}
```

**Пример запроса (возведение в степень):**
```bash
curl -X POST http://localhost:5000/api/calculate \
     -H "Content-Type: application/json" \
     -H "X-API-Key: secret_key_12345" \
     -d '{
       "operation": "power",
       "a": 2,
       "b": 8
     }'
```

**Пример ответа:**
```json
{
  "result": 256,
  "operation": "power",
  "a": 2,
  "b": 8,
  "expression": "2 ^ 8 = 256",
  "id": 3
}
```

**Ошибка: Деление на ноль**
```json
{
  "error": "Деление на ноль невозможно!",
  "operation": "divide",
  "a": 10,
  "b": 0
}
```
Статус код: `400 Bad Request`

**Ошибка: Неверный API-ключ**
```json
{
  "error": "Неверный или отсутствующий API-ключ"
}
```
Статус код: `401 Unauthorized`

**Ошибка: Отсутствует обязательное поле**
```json
{
  "error": "Отсутствует обязательное поле: operation"
}
```
Статус код: `400 Bad Request`

---

### 4. Получение истории вычислений

**GET** `/api/history`

Возвращает историю всех выполненных вычислений.

**Заголовки:**
```
X-API-Key: secret_key_12345
```

**Параметры запроса (опционально):**
- `limit` - количество последних записей (например, `?limit=5`)

**Пример запроса (вся история):**
```bash
curl -H "X-API-Key: secret_key_12345" \
     http://localhost:5000/api/history
```

**Пример запроса (последние 3 записи):**
```bash
curl -H "X-API-Key: secret_key_12345" \
     "http://localhost:5000/api/history?limit=3"
```

**Пример ответа:**
```json
{
  "history": [
    {
      "id": 1,
      "operation": "add",
      "a": 10,
      "b": 5,
      "result": 15,
      "expression": "10 + 5 = 15"
    },
    {
      "id": 2,
      "operation": "divide",
      "a": 15,
      "b": 3,
      "result": 5,
      "expression": "15 / 3 = 5"
    },
    {
      "id": 3,
      "operation": "power",
      "a": 2,
      "b": 8,
      "result": 256,
      "expression": "2 ^ 8 = 256"
    }
  ],
  "total": 3,
  "returned": 3
}
```

---

### 5. Удаление записи из истории

**DELETE** `/api/history/<id>`

Удаляет конкретную запись из истории вычислений по её ID.

**Заголовки:**
```
X-API-Key: secret_key_12345
```

**Пример запроса:**
```bash
curl -X DELETE \
     -H "X-API-Key: secret_key_12345" \
     http://localhost:5000/api/history/1
```

**Пример ответа:**
```json
{
  "message": "Запись с ID 1 успешно удалена",
  "deleted_entry": {
    "id": 1,
    "operation": "add",
    "a": 10,
    "b": 5,
    "result": 15,
    "expression": "10 + 5 = 15"
  }
}
```

**Ошибка: Запись не найдена**
```json
{
  "error": "Запись с ID 999 не найдена"
}
```
Статус код: `404 Not Found`

---

### 6. Очистка всей истории

**DELETE** `/api/history`

Удаляет все записи из истории вычислений.

**Заголовки:**
```
X-API-Key: secret_key_12345
```

**Пример запроса:**
```bash
curl -X DELETE \
     -H "X-API-Key: secret_key_12345" \
     http://localhost:5000/api/history
```

**Пример ответа:**
```json
{
  "message": "История очищена. Удалено записей: 5"
}
```

---

## Коды ответов

| Код | Описание |
|-----|----------|
| 200 | Успешный запрос |
| 400 | Ошибка в данных запроса |
| 401 | Неверный или отсутствующий API-ключ |
| 404 | Ресурс не найден |
| 500 | Внутренняя ошибка сервера |

---

## Запуск API

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Запуск сервера

```bash
python api.py
```

Сервер запустится на `http://localhost:5000`

### Настройка через переменные окружения

```bash
# Изменить порт
export PORT=8080

# Изменить API-ключ
export CALCULATOR_API_KEY=my_secret_key

# Включить режим отладки
export FLASK_DEBUG=true

python api.py
```

---

## Примеры использования

### Python (requests)

```python
import requests

API_URL = "http://localhost:5000"
API_KEY = "secret_key_12345"

headers = {
    "Content-Type": "application/json",
    "X-API-Key": API_KEY
}

# Выполнение вычисления
data = {
    "operation": "multiply",
    "a": 7,
    "b": 6
}

response = requests.post(
    f"{API_URL}/api/calculate",
    json=data,
    headers=headers
)

result = response.json()
print(f"Результат: {result['result']}")
print(f"Выражение: {result['expression']}")
```

### JavaScript (fetch)

```javascript
const API_URL = "http://localhost:5000";
const API_KEY = "secret_key_12345";

// Выполнение вычисления
async function calculate(operation, a, b) {
    const response = await fetch(`${API_URL}/api/calculate`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'X-API-Key': API_KEY
        },
        body: JSON.stringify({
            operation: operation,
            a: a,
            b: b
        })
    });
    
    const result = await response.json();
    return result;
}

// Использование
calculate('add', 10, 5).then(result => {
    console.log(`Результат: ${result.result}`);
    console.log(`Выражение: ${result.expression}`);
});
```

---

## Тестирование с помощью Postman

### Настройка коллекции

1. Создайте новую коллекцию "Calculator API"
2. Добавьте переменную коллекции:
   - `base_url`: `http://localhost:5000`
   - `api_key`: `secret_key_12345`

### Примеры запросов для Postman

#### 1. Проверка работоспособности
- **Метод:** GET
- **URL:** `{{base_url}}/api/health`
- **Headers:** не требуются

#### 2. Получение списка операций
- **Метод:** GET
- **URL:** `{{base_url}}/api/operations`
- **Headers:**
  - `X-API-Key`: `{{api_key}}`

#### 3. Выполнение вычисления (сложение)
- **Метод:** POST
- **URL:** `{{base_url}}/api/calculate`
- **Headers:**
  - `Content-Type`: `application/json`
  - `X-API-Key`: `{{api_key}}`
- **Body (raw JSON):**
```json
{
  "operation": "add",
  "a": 10,
  "b": 5
}
```

#### 4. Получение истории
- **Метод:** GET
- **URL:** `{{base_url}}/api/history`
- **Headers:**
  - `X-API-Key`: `{{api_key}}`

#### 5. Удаление записи
- **Метод:** DELETE
- **URL:** `{{base_url}}/api/history/1`
- **Headers:**
  - `X-API-Key`: `{{api_key}}`

---

## Обработка ошибок

API возвращает подробные сообщения об ошибках в формате JSON:

```json
{
  "error": "Описание ошибки",
  "additional_info": "Дополнительная информация (если есть)"
}
```

### Типичные ошибки:

1. **Неверный API-ключ** (401)
2. **Отсутствуют обязательные поля** (400)
3. **Неверный тип данных** (400)
4. **Деление на ноль** (400)
5. **Неизвестная операция** (400)
6. **Ресурс не найден** (404)

---

*Версия API: 1.0*  
*Дата последнего обновления: 2026*
