# 🤖 Интеграция с Telegram

## Описание

Проект калькулятора интегрирован с Telegram-ботом для отправки уведомлений о выполненных вычислениях и интерактивной работы через Telegram.

## Возможности

1. **Telegram-бот** - интерактивный бот для выполнения вычислений
2. **Интеграция с API** - автоматическая отправка уведомлений при вычислениях через API
3. **История вычислений** - сохранение последних результатов для каждого пользователя

---

## Настройка

### Шаг 1: Создание Telegram-бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен

### Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 3: Настройка переменных окружения

```bash
# Токен бота (обязательно)
export TELEGRAM_BOT_TOKEN=your_bot_token_here

# ID чата для уведомлений (опционально, для API интеграции)
export TELEGRAM_CHAT_ID=your_chat_id

# Включить отправку уведомлений через API (опционально)
export TELEGRAM_ENABLED=true
```

### Шаг 4: Получение Chat ID

**Способ 1: Через скрипт**
```bash
TELEGRAM_BOT_TOKEN=your_token python telegram_integration.py
```
Отправьте любое сообщение боту, и скрипт покажет ваш Chat ID.

**Способ 2: Через бота @userinfobot**
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот покажет ваш Chat ID

---

## Использование

### Запуск Telegram-бота

```bash
TELEGRAM_BOT_TOKEN=your_token python telegram_bot.py
```

После запуска найдите вашего бота в Telegram и отправьте `/start`.

### Команды бота

- `/start` - Приветственное сообщение
- `/help` - Справка по использованию
- `/calculate <число1> <операция> <число2>` - Выполнить вычисление
- `/history` - Показать последний результат

### Примеры использования

**Простой ввод:**
```
10 + 5
```
Ответ: `✅ Результат: 10 + 5 = 15`

**Команда:**
```
/calculate 15 / 3
```
Ответ: `✅ Результат: 15 ÷ 3 = 5`

**Возведение в степень:**
```
2 ^ 8
```
Ответ: `✅ Результат: 2 ^ 8 = 256`

---

## Интеграция с API

### Автоматические уведомления

При выполнении вычисления через API можно автоматически отправлять уведомление в Telegram.

**Настройка:**

1. Установите переменные окружения:
```bash
export TELEGRAM_BOT_TOKEN=your_token
export TELEGRAM_CHAT_ID=your_chat_id
export TELEGRAM_ENABLED=true
```

2. Запустите API:
```bash
python api.py
```

3. При выполнении вычисления через API уведомление автоматически отправится в Telegram.

**Пример запроса с уведомлением:**
```bash
curl -X POST http://localhost:5001/api/calculate \
     -H "Content-Type: application/json" \
     -H "X-API-Key: secret_key_12345" \
     -H "X-Telegram-Chat-ID: your_chat_id" \
     -d '{"operation": "add", "a": 10, "b": 5}'
```

### Программная отправка уведомлений

```python
from telegram_integration import send_notification_sync

# Отправка уведомления
send_notification_sync(
    expression="10 + 5 = 15",
    result=15.0,
    chat_id="123456789"
)
```

---

## Тестирование

### Запуск тестов

```bash
python test_telegram_integration.py
```

### Ручное тестирование

1. **Запустите бота:**
   ```bash
   TELEGRAM_BOT_TOKEN=your_token python telegram_bot.py
   ```

2. **Откройте Telegram и найдите вашего бота**

3. **Отправьте команду `/start`**

4. **Попробуйте различные вычисления:**
   - `10 + 5`
   - `15 / 3`
   - `2 ^ 8`
   - `/calculate 7 * 6`

5. **Проверьте команду `/history`**

### Тестирование интеграции с API

1. **Настройте переменные окружения:**
   ```bash
   export TELEGRAM_BOT_TOKEN=your_token
   export TELEGRAM_CHAT_ID=your_chat_id
   export TELEGRAM_ENABLED=true
   ```

2. **Запустите API:**
   ```bash
   python api.py
   ```

3. **Выполните вычисление через API:**
   ```bash
   curl -X POST http://localhost:5001/api/calculate \
        -H "Content-Type: application/json" \
        -H "X-API-Key: secret_key_12345" \
        -d '{"operation": "multiply", "a": 7, "b": 6}'
   ```

4. **Проверьте Telegram - должно прийти уведомление**

---

## Обработка ошибок

### Отсутствие токена

Если `TELEGRAM_BOT_TOKEN` не установлен, бот не запустится, а уведомления не будут отправляться. Ошибки логируются, но не прерывают работу API.

### Ошибки сети

При ошибках сети (недоступность Telegram API) ошибки логируются, но не влияют на работу основного функционала.

### Дублирование уведомлений

Уведомления отправляются только при успешном выполнении вычисления. Повторные запросы с теми же данными создадут новые записи в истории, но уведомления будут отправлены для каждого вычисления.

---

## Структура файлов

- `telegram_bot.py` - Основной файл Telegram-бота
- `telegram_integration.py` - Модуль для интеграции с API
- `test_telegram_integration.py` - Тесты для интеграции
- `TELEGRAM_INTEGRATION.md` - Документация (этот файл)

---

## Пример отчета

**Интеграция выполнена:**

✅ Telegram-бот создан и протестирован
✅ Бот успешно выполняет вычисления по командам
✅ Интеграция с API реализована - уведомления отправляются автоматически
✅ Обработка ошибок реализована корректно
✅ Тесты пройдены успешно

**Использованные технологии:**
- python-telegram-bot (SDK для Telegram Bot API)
- Асинхронное программирование (asyncio)
- Интеграция с Flask API

**Результаты тестирования:**
- Бот корректно обрабатывает команды
- Уведомления отправляются при вычислениях через API
- Ошибки обрабатываются без прерывания работы системы
- Дублирование уведомлений не происходит

---

*Версия: 1.0*  
*Дата: 2026*
